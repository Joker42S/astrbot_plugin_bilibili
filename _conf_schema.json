{
    "sessdata": {
        "description": "sessdata",
        "type": "string",
        "hint": "sessdata 获取请查看 readme",
        "obvious_hint": true
    },
    "interval_mins": {
        "description": "interval_mins",
        "type": "int",
        "hint": "订阅检查间隔时间，分钟数",
        "obvious_hint": true,
        "default": 5
    },
    "rai": {
        "description": "render_as_image",
        "type": "bool",
        "hint": "是否以图片形式渲染输出(仅对图文动态生效)",
        "default": true
    },
    "node": {
        "description": "node",
        "type": "bool",
        "hint": "是否以转发消息形式推送(仅对图文动态生效)",
        "default": false
    },
    "enable_parse_miniapp": {
        "description": "enable_parse_miniapp",
        "type": "bool",
        "hint": "是否解析小程序",
        "default": true
    },
    "bili_t2i":{
        "description": "bili_t2i",
        "type": "string",
        "hint": "设置自部署t2i服务接口以获得有限的清晰度提升（可选）",
        "default": ""
    },
    "max_msg_count":{
        "description": "max_msg_count",
        "type": "int",
        "hint": "订阅的每个up每次最多会推送几条新动态，例如：设置为5，检查订阅时一位up主发送了10条未推送的新动态，此时只会推送通过筛选的最新的5条动态",
        "default": 5
    }
}
